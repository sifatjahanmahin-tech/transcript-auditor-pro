// Authentication is handled by the backend (FastAPI + Google OAuth).
// This file is intentionally a stub — NextAuth is not used.
export function GET() {
  return new Response("Not used", { status: 404 });
}
export function POST() {
  return new Response("Not used", { status: 404 });
}
